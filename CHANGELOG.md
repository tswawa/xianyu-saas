# 更新记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号按 [Semantic Versioning](https://semver.org/lang/zh-CN/) 管理。

当前维护版本为 `0.4.6`，更早版本停止维护。

## [0.4.6] - 2026-09-20

### 新增与优化

- **待发货订单补发**：店铺连接后主动检查已付款、待发货订单，默认每 5 分钟补查 1 页、最多 30 条；通过订单核验后按商品配置发货，不再只依赖付款消息触发。
- **发送队列**：同一店铺的自动回复、人工回复与发货消息按顺序发送，统一控制发送间隔；发送前重新检查当前会话是否已被人工接管。
- **客服开关分离**：规则客服与 AI 客服可以分别启停，自动发货独立运行。已配置发货的店铺，即使关闭客服也能继续处理订单。
- **客服人设重构**：保留小喵客服，新增薄荷客服和自定义人设；精简为名称与人设说明，扩大编辑区域。切换预设自动保存，手动修改后点击「保存并生效」。
- **模型回复更自然**：移除关键词触发的固定拒绝话术，以及旧称呼、语气、长度和表情选项的隐藏提示；普通对话由模型结合当前人设、商品知识和历史生成回复。
- **回复与预览**：较长 AI 回复可拆成短消息依次发送，模型暂时限流或超时会有限重试；沙盘展示实际回复及本次未回复原因，不再把内部决策内容显示成买家回复。
- **买家会话更清楚**：优先显示买家昵称；支持直接粘贴图片，发送图片时自动人工接管，点击「恢复 AI」即可恢复自动回复。
- **概览更紧凑**：保留两列布局，缩短卡片高度；待处理、最近订单与商品区限制展示数量，滚动查看最新内容，商品缩略图最多展示 4 个。
- **商品设置更直接**：简化商品资料与批量发货的保存步骤；保存后尝试启动自动发货，分别提示配置保存结果与店铺运行状态。
- **文档与截图**：更新完整功能介绍、首次管理员注册、Docker 与 Ubuntu 安装和网页更新说明，重新制作 6 张横屏截图。

### 修复

- 修复付款后未触发自动发货、短编号订单被漏掉的问题；补查与实时消息共用发货记录和核验流程，避免重复分配卡密与重复发送。
- 修复补发途中断线后订单卡在「发送中」的问题，重连后可沿用原有库存预留继续处理。
- 修复店主向其他卖家咨询的会话被当成买家咨询、触发自动客服的问题；这些会话也不再出现在买家会话列表中。
- 修复二维码被页面安全策略拦截、登录过期后重试不显示二维码，以及扫码完成后的过渡状态错误。
- 修复断开连接后旧进程未及时停止、重新授权后 Worker 状态未恢复的问题；升级时保留旧版手动停止店铺的状态。
- 修复 Linux 进程编号复用时误认遗留 Worker 的问题；断线期间收到的过期咨询保留供人工查看，不在恢复后集中自动回复。
- 修复客服配置保存出现 422、开关与实际保存状态不一致，以及旧模板覆盖当前人设说明的问题。
- 修复图片上传与发送异常、消息图片下方重复显示「图片」文字，以及部分待处理入口跳错页面的问题。

### 升级

已接入内置更新功能的 **v0.4.5** 可在网页点击「更新」，下载校验后输入管理员密码，程序自动安装并重启；完成后刷新页面。普通代码更新无需重建 Docker 镜像，现有账号、店铺配置与业务数据保留。

后续维护以 **v0.4.6** 为起点。本版新增配置不能被旧版完整识别，升级后不建议降级。未接入内置更新功能的历史安装，请按部署指南迁移。

本版不会删除历史消息，但未能确认属于当前店铺商品的旧会话会被隐藏。旧的称呼、语气、回复长度和表情档位停止生效，可在新的人设说明中统一调整。

## [0.4.5] - 2026-09-16

- 网页下载并校验更新包，确认后自动切换版本和重启。普通代码更新无需重建 Docker 镜像。
- Docker 与 Ubuntu 安装包内置更新启动器，Ubuntu 保留 x86_64 和 ARM64。未接入更新组件的旧安装仍需先迁移。
- 更新按钮统一为「更新」，显示真实下载大小与百分比；关闭弹窗后可重新打开查看进度。
- 下载失败可重试，新版本启动失败时尝试恢复上一版本，保留业务数据与配置。文件更新不自动备份数据库。

## 历史测试说明

0.4.5-update-test.1 至 test.7 的有效改动已合并至 0.4.5。0.3.2、0.4.1、0.4.3 仅用于验证版本更新，没有新增业务功能。

以上测试 Release 归档后，重新安装或修复请使用正式版安装器；已接入更新组件的实例仍可检查后续版本。

## [0.4.4] - 2026-09-14

本版本整合 Docker 安装器、实现卡密保存写入失败恢复，并优化更新就绪提示与安装流程。本批改动未重新实现 Ubuntu 更新架构（继续沿用已验证的引导管理器与独立运行包架构）。

### 新增

- **Docker 安装器整合与就绪验收**：新增并完善 `deploy/docker-install.sh`。从 Release 源码包构建应用与独立更新器镜像，将包内受信任公钥预装为容器内 root 拥有的副本（0644），仅为全新数据目录设置容器用户属主；在一次性登记（`initialize`）后调用应用侧 `platform_update.update_capabilities()` 验收真实更新就绪能力，避免仅凭 `/health` 误判。重复运行已登记安装只校验并启动既有容器（`docker start`），不重建、不应用新 env/Compose、不覆盖升级镜像或现有数据。

### 修复

- **卡密保存写入失败恢复**：卡密（`redeem_codes.json`）与卡池（`card_pool.json`）两个文件的保存改用补偿写入上下文（`AccountStorage.compensating_write`）。任一文件写入异常时自动恢复旧快照字节、移除本次新建的文件；若补偿恢复失败抛出 `AccountStorageRecoveryError`（HTTP 503），明确提示人工核对，避免部分写入破坏数据一致性。
- **更新提示与更新能力状态区分**：前端 `UPDATE_UI_COPY` 增加 `update_installation_migration_required` 状态提示；更新能力不可用时统一回退至明确的原因提示，避免未就绪时错误显示“未检测到独立更新器”。
- **无 Git 源码包构建元数据保留**：Release 源码包在 Docker 构建时保留包内同版本的构建元数据（提交哈希、时间戳、干净标记），显式构建参数优先，避免安装镜像丢失发布版本信息。
- **前端静态资源标识刷新**：统一刷新静态资源版本标识为 `20260914-01`，确保浏览器脚本与样式缓存正确更新。

### 已知限制与边界

- **架构延续说明**：Ubuntu 原生更新架构本批未重新实现，继续沿用 v0.4.2 引入的引导管理器与独立运行包。
- **自动升级范围**：既有未接入独立更新器的 Docker 源码部署、systemd 部署与普通源码运行不会因升级到 0.4.4 自动获得网页升级能力，仍需按部署文档人工维护；本版本不包含自动迁移。
- **数据保留与容器行为边界**：Docker 网页升级只切换运行镜像，受管重跑仅校验并启动既有容器（不切换镜像、不加载新配置）；挂载的 `./data` 业务数据始终保留。Docker 升级时不自动备份数据库，升级前需自行冷备 `./data` 目录；Ubuntu 原生升级包含数据库备份步骤（数据保存在 `/var/lib/xianyu-saas`）。
- **运行环境限制**：Docker 更新器固定要求 Docker Engine API v1.47；安装脚本仅支持 Linux 或 WSL2 Linux 环境中的本地 Docker 引擎，不支持在 Windows PowerShell 或远程 Docker 上下文中直接运行。Ubuntu 原生安装器仅支持 Ubuntu 22.04、24.04 与 Debian 12。

## [0.4.2] - 2026-09-14

- systemd 首次安装已完整集成独立签名的 manager 与 updater 组件，并新增基于官方 GitHub 仓库的一行 root 快速安装入口。
- 支持对官方 v0.4.0 位于 `/opt` 或 `/srv` 的既有部署执行一次性接入，完整保留现有环境配置文件、SQLite 数据库、租户数据目录与旧版源码。
- 网页端升级支持签名校验、维护态隔离、备份与数据迁移、版本切换、健康检查与失败回退。
- 修复独立包内 Python 可执行权限丢失、Ubuntu 等 Linux 环境下 manager 运行兼容性，以及接入旧环境时外部 `/srv` 数据路径被覆盖的问题。

## [0.4.0] - 2026-09-13

- 新增 Linux x86_64 与 aarch64 架构独立安装包（standalone bundle）及配套的稳定 updater manager。
- 首次安装无需预装系统级 Python、Node.js、Nginx 或 Docker，仅需具备 systemd 环境、root 执行权限以及网络连接。
- 网页端升级机制涵盖签名校验、维护态隔离、自动备份与迁移、版本切换、健康检查与失败回退。
- 支持从既有源码部署或旧版 systemd 架构平滑迁移，完整保留既有环境配置与业务数据。
- 建议在生产环境执行迁移或升级前，先在测试机器完成完整数据备份与升级验证。

## [0.3.1] - 2026-09-13

> 本次发布包含独立更新器与新版版本弹层。Docker 和 Linux systemd 受管部署可接入管理员后台更新；普通源码启动仍须手动更新。旧版本（v0.2.0/v0.2.2）必须先由维护者完成一次性更新器部署、可信基线接入与初始化，不能仅靠新版本发布自动获得安装能力。已完成的隔离验证不等同于所有生产环境验收；请先备份并试用，部署文档的全面更新将在试用确认后进行。

### 新增

- **低频版本自动探测**：服务端默认每 6 小时（21600 秒，`SAAS_UPDATE_CHECK_INTERVAL_SECONDS`）异步探测一次发布版本。通过全站共享数据库缓存与租约合并并发请求，失败退避保留已知更新结果，多租户多标签页不重复访问 GitHub；已登录页面可见时每 5 分钟读取本地缓存并在版本徽标变黄提示。自动探测只发现版本，不执行静默安装。
- **管理员安全升级交互**：版本徽标弹层新增管理员升级入口与原生无障碍弹窗（`<dialog id="platformUpdateDialog">`）。流程包含：升级制品下载与签名校验、管理员密码验证及升级风险确认、提交升级请求（HTTP 202 仅代表已受理）、真实执行阶段与重连反馈。管理员密码与确认令牌不写入浏览器持久存储，关闭弹窗不取消已提交的后台操作。
- **Docker Compose 独立更新器**：提供需最后叠加的 `docker-compose.updates.yml`，由独立更新器校验签名源码、构建镜像并使用官方 Compose 5.5.1 仅重建应用（`--no-deps`）。Web 应用不挂载 Docker socket；高权限执行仅交给不对外发布端口的独立更新器。首次接入需在宿主机通过 `initialize` 登记完整配置，凭据仅保留在更新器私有卷。升级先进入维护并排空任务；失败时用原配置和同一数据卷重建旧镜像，不恢复旧业务数据。已完成隔离 Docker 升级、失败回退和同卷保留验证；升级前仍须由维护者备份业务数据，不自动恢复旧数据库或处理不兼容的数据版本。
- **systemd 独立执行器规范化**：更新器独立于活动代码软链接之外运行。首次接入需由 root 依序完成独立 bundle 安装、离线基线导入（`--import-trusted-baseline` 经 AST 静态核验 `MAINTENANCE_PROTOCOL=1`，不自动切换 current/不触碰业务数据）、人工切换现役软链接，并通过显式受控环境变量执行 `--initialize`（配置 sticky `01770` 专用 IPC 目录并原子写入可信接入记录作为控制面升级门禁）。旧安装实例需由运维完成一次性受控基线接入，具体部署路径与权限规范参见 [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md)。
- **发布制品契约扩展**：本次发布包含 10 项资产（在原有 8 项基础上增加对应版本的 `.docker.manifest.json` 与 `.docker.manifest.sig`；`artifacts.json` 记录 8 项内容资产，`SHA256SUMS` 覆盖除自身外的 9 项文件；历史 v0.2.2 保持 8 项资产事实不变）。
- **版本弹层优化**：重排版本、检查状态与发布入口，无更新时隐藏“立即更新”；回退列表仅展示受信候选。保留管理员密码和风险确认，并完成窄屏、文字缩放、键盘与焦点交互验收。静态资源标识更新为 `20260913-01`。

### 修复

- **中断恢复验收的时序稳定性**：测试专用更新器在持久化检查点一次性阻塞，再立即强制重启，避免故障注入错过目标阶段；超时记录最后阶段，原有镜像回退与数据保留断言保持不变。生产更新器逻辑未因此修改。

## [0.3.0] - 2026-09-13（未发布候选）

> 此版本已推送候选标签，但标签 CI 的中断恢复场景等待超时，未正式发布 Release。已推送标签保留，功能整体并入 `0.3.1`。

## [0.2.2] - 2026-09-11

包含原 0.2.1 候选版本的全部新增功能与修复，并修复了规则草稿在异步响应下的竞态覆盖问题。

### 新增

- **账号初始化安全补齐**：注册与首次管理员创建支持补齐空目录与纯默认配置残留，拒绝业务数据与损坏配置，接入并发回滚保护；权限边界保持独立不变。
- **智能客服沙盘草稿模拟**：支持基于未保存草稿与空内容快速模拟（标记 `sent: false`，不保存配置、不发闲鱼消息）。买家问题必填，模型连接须有效。正式客服仅要求保存有效店铺客服内容并显式启用，商品补充知识非必填。沙盘不参与关键词规则与发货履约。
- **扫码租约与频控保护**：扫码前先核验真实冷却并预留有限连接租约，后台旧会话检测延后，临时等待不消耗重试预算。前端倒计时禁用重复操作，归零不自动连续发起请求。明确时间语义：扫码确认后会话有效期默认 90 秒；店铺同步冷却（`sync_cooldown`）默认 60 秒；风控后的本地保护（`risk_cooldown`）为 600 秒熔断，避免两类冷却混淆。按剩余有效期决定复用或重新扫码，新 Cookie 验证通过后替换旧连接。

### 改进

- **工作台首页视觉优化**：移除 4 个快捷入口与待办空态对勾图标；调整运行情况卡片标题、作用域标签与窄屏响应式排版；同步更新 12 张文档演示截图。
- **静态资源版本更新**：代码静态资产标识更新为 `20260911-01`，确保前端脚本与样式缓存正确刷新。

### 修复

- **回复规则草稿异步竞态保护**：同店铺规则列表 GET 响应延迟到达时不再清空新建草稿；编辑已有规则时保留索引对应的已有列表，新建草稿仍正常加载服务器已有规则。
- **模型连接错误会话隔离**：上游模型返回 401 或 403 时通过非本站 401 错误包装返回，修复误触发工作台退出登录的问题。请求失败后按钮恢复可用，历史请求不污染新会话。
- **网络代理与 DNS 兼容说明**：纠正 Fake-IP 环境配置建议，明确大模型域名须通过真实公网 DNS 解析；禁止建议关闭 SSRF 防护或跳过 TLS 校验，禁止将本机临时 IP 映射写入通用部署包。
- **运行环境说明纠偏**：明确 Windows 宿主机须通过 Docker Linux 容器运行完整服务，纠正 Windows 原生可直接运行全部服务的误述。
- **发布资产清单校验**：修正发布资产说明，明确 `SHA256SUMS` 覆盖 6 项内容资产与 `artifacts.json` 共 7 项制品（不含自身）。

### 发布包与更新说明

- **源码与手动部署**：提供完整源码包 `xianyu-saas-0.2.2-source.zip`，适用于 Docker 构建或手动部署。
- **systemd 签名更新**：已部署实例使用同版本 `xianyu-saas-0.2.2.tar.gz`、`.manifest.json` 与 `.manifest.sig` 执行升级，公钥来自 `deploy/update-signing.pub`，文件完整性参考 `SHA256SUMS`。
- **Docker 容器更新**：升级通过在宿主机重新构建并启动容器完成，保留原有数据卷与挂载，Web 界面不接管宿主机容器生命周期。
- **参考文档**：打包机制参见 [`docs/RELEASING.md`](docs/RELEASING.md)，生产部署参见 [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md)。

## [0.2.1] - 2026-09-11（未发布候选）

> 说明：本版本作为发布候选推送到 `v0.2.1` 标签，因标签 CI 在规则草稿异步回归测试中失败而未正式发布 Release，已推送标签保留，对应功能与修复整体并入 `0.2.2`。

### 新增

- **账号初始化安全补齐**：注册与首次管理员创建支持补齐空目录与纯默认配置残留，拒绝业务数据与损坏配置，接入并发回滚保护；权限边界保持独立不变。
- **智能客服沙盘草稿模拟**：支持基于未保存草稿与空内容快速模拟（标记 `sent: false`，不保存配置、不发闲鱼消息）。买家问题必填，模型连接须有效。正式客服仅要求保存有效店铺客服内容并显式启用，商品补充知识非必填。沙盘不参与关键词规则与发货履约。
- **扫码租约与频控保护**：扫码前先核验真实冷却并预留有限连接租约，后台旧会话检测延后，临时等待不消耗重试预算。前端倒计时禁用重复操作，归零不自动连续发起请求。明确时间语义：扫码确认后会话有效期默认 90 秒；店铺同步冷却（`sync_cooldown`）默认 60 秒；风控后的本地保护（`risk_cooldown`）为 600 秒熔断，避免两类冷却混淆。按剩余有效期决定复用或重新扫码，新 Cookie 验证通过后替换旧连接。

### 改进

- **工作台首页视觉优化**：移除 4 个快捷入口与待办空态对勾图标；调整运行情况卡片标题、作用域标签与窄屏响应式排版；同步更新 12 张文档演示截图。

### 修复

- **模型连接错误会话隔离**：上游模型返回 401 或 403 时通过非本站 401 错误包装返回，修复误触发工作台退出登录的问题。请求失败后按钮恢复可用，历史请求不污染新会话。
- **网络代理与 DNS 兼容说明**：纠正 Fake-IP 环境配置建议，明确大模型域名须通过真实公网 DNS 解析；禁止建议关闭 SSRF 防护或跳过 TLS 校验，禁止将本机临时 IP 映射写入通用部署包。
- **运行环境说明纠偏**：明确 Windows 宿主机须通过 Docker Linux 容器运行完整服务，纠正 Windows 原生可直接运行全部服务的误述。
- **发布资产清单校验**：修正发布资产说明，明确 `SHA256SUMS` 覆盖 6 项内容资产与 `artifacts.json` 共 7 项制品（不含自身）。

### 发布包与更新说明

- **源码与手动部署**：提供完整源码包 `xianyu-saas-0.2.1-source.zip`，适用于 Docker 构建或手动部署。
- **systemd 签名更新**：已部署实例使用同版本 `xianyu-saas-0.2.1.tar.gz`、`.manifest.json` 与 `.manifest.sig` 执行升级，公钥来自 `deploy/update-signing.pub`，文件完整性参考 `SHA256SUMS`。
- **Docker 容器更新**：升级通过在宿主机重新构建并启动容器完成，保留原有数据卷与挂载，Web 界面不接管宿主机容器生命周期。
- **参考文档**：打包机制参见 [`docs/RELEASING.md`](docs/RELEASING.md)，生产部署参见 [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md)。

## [0.2.0] - 2026-09-10

### 新增

- **用户统一模型连接**：系统设置提供用户级统一大模型连接配置（`/api/settings/ai/connection`），前端输入不回显且不长期缓存；服务端基于 32 字节随机二进制经标准 Base64 编码（44 字符，示例：`base64.b64encode(os.urandom(32)).decode()`）的主密钥（`SAAS_AI_MASTER_KEY`）派生子密钥，采用 AES-GCM 加密存储，供多店铺的 AI 客服、知识提炼与店铺助手共享。
- **持久店铺助手（Operations Agent）**：通过 `/api/ops/*` 提供基于多轮会话的店铺运维支持，调用受限工具集执行已授权的知识、规则与发货配置，具备操作租约、执行回执记录、步骤取消、追问确认与失败重试恢复能力；禁止执行真实发货，禁止向买家发送消息，不执行 shell 命令，不导出卡密明文。
- **资源监控与店铺管理**：基于 Linux `/proc` 接口实时采样各 Worker 运行状态（单核基准 CPU 百分比、物理内存 RSS、地址空间上限 `RLIMIT_AS`）。单 Worker 地址空间默认上限 400 MiB，单用户默认上限 20 个店铺（数据库设置优先于初始环境变量）。
- **商品与发货管理**：支持卡片与列表双视图切换，支持请求触发的异步商品快照拉取；发货模板支持固定文字资料（仅支持单件）、网盘资源与卡密库存绑定；发货前通过平台双接口（`HeadInfo` 与 `OrderDetail`）核验待发货状态（`status == 2`），仅卡密类型扣减库存。
- **说明文档与功能演示**：整理完整说明文档体系与工作台 12 张核心业务功能演示截图。

### 修复

- **请求受限与防护状态细化**：细化区分服务繁忙、访问受限与平台明确验证要求，历史 `risk_control` 状态不再断言 App 需要验证，同时保留原有访问受限保护机制。
- **未绑定模板与映射隔离**：允许合法未绑定商品的模板作为草稿保留，不影响其他商品的规则与发货映射。
- **卡密库存同步与状态保持**：支持清单中尚未导入 Worker 库的新卡密正确识别可用库存量，已使用、已预留与已撤销状态保持有效并不发生状态复活。
- **店铺助手操作范围约束**：完善同店铺内目标商品范围识别与变更校验，准确处理排除商品、引用名称、ID 列表匹配与逐商品替换确认，防止操作范围非预期扩大。

### 发布包与更新说明

- **源码与手动部署**：v0.2.0 提供完整源码包 `xianyu-saas-0.2.0-source.zip`，适用于 Docker 构建或手动部署。
- **systemd 签名更新**：已部署的 systemd 实例使用同版本 `xianyu-saas-0.2.0.tar.gz`、`xianyu-saas-0.2.0.manifest.json` 与 `xianyu-saas-0.2.0.manifest.sig` 执行升级。首次启用前需从受信来源将公钥 `deploy/update-signing.pub` 配置到 `SAAS_UPDATE_PUBLIC_KEY_FILE`；核验下载文件完整性请参考 `SHA256SUMS`。
- **Docker 容器更新**：Docker 部署需先备份现有数据，使用原 Compose 配置文件及本地覆盖配置重新构建并启动容器，继续挂载并保留原数据卷。
- **参考文档**：打包流程与签名机制参见 [`docs/RELEASING.md`](docs/RELEASING.md)，生产部署与维护细节参见 [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md)。

## 历史开发记录 - 2026-09-05（原 0.2.0 开发标记）

### 新增

- 人工回复支持一次提交最多 8 张图片或图片加文字；图片按顺序逐张发送，文字最后单独发送，并为每个部分记录协议 ACK。
- 增加人工回复 outbox 的分段恢复、未确认部分重试、共享图片引用保护和过期文件清理。
- 提供 Docker Compose 运行方式，Linux、Windows 和 macOS 可按宿主环境选择容器路径。

### 变更

- 工作台和文档统一采用店铺账号作用域；历史订阅字段仅保留兼容用途，不作为当前功能分级。
- AI 客服以店铺与商品的自然语言内容为主，支持 OpenAI 兼容接口、OpenAI Responses、Anthropic Messages、Google Gemini 和 Ollama。
- 部署说明明确区分 Docker 自托管与 Linux systemd 版本化发布；两种方式不在同一实例混用。
- 公开文档改为面向使用者和贡献者的说明，不包含运行环境中的账号、凭据、业务数据或本机协作资料。

### 修复

- 修正人工回复在多媒体、失败重试和进程恢复场景下可能出现的顺序、重复确认和本地临时文件清理边界。
- 收紧异常 outbox 记录的隔离处理，单条坏记录不会阻塞后续正常回复。

当前阶段仍需在目标环境单独验证容器构建、真实闲鱼账号、第三方模型、真实订单履约以及 nginx/systemd 生产部署；离线合同不替代这些验证。

## [0.1.0] - 2026-08-28

首个功能基线，覆盖六个工作台业务域：运营概览、智能客服、履约中心、订单管理、店铺管理和项目说明。

### 新增

- 服务端官方二维码店铺连接、账号切换、商品同步和状态恢复。
- SQLite jobs/leases、Worker 期望状态、消费者重试/死信和重启恢复边界。
- 统一收件箱、会话搜索、未读状态、人工接管和持久人工回复 outbox。
- 自然语言内容驱动的 AI 客服与连续对话沙盘。
- 五种 provider 适配格式：OpenAI Chat Completions、OpenAI Responses、Anthropic Messages、Google Gemini 和 Ollama Chat。
- 商品客服内容、兑换码/网盘资源配置和基于订单证明的受控履约流程。
- nginx、systemd、日志轮转、Docker 和开发环境模板。
- CI、贡献指南、行为准则、Issue/PR 模板与许可证边界文档。

### 安全边界

- 浏览器不保存平台 Cookie、Token 或长期模型密钥；敏感凭据只在服务端受控保存和使用。
- 控制文件缺失或损坏时安全关闭自动化；默认配置不包含真实账号、商品、库存或订单数据。
- AI 只能生成客服文本或人工接管/不回复决策，不能授权发货、改变订单或跨店铺读取资料。
- 自动履约必须核验订单号、商品、买家、卖家、状态和数量。
- 生产配置、运行数据和备份不进入 Git 或发布包。

### 验证边界

- 仓库提供 API、账号隔离、AI、Worker、部署配置和桌面/移动 UI 的离线合同。
- 默认测试使用脱敏样例、临时目录和模拟上游。
- 真实平台登录、第三方模型质量、真实订单履约、TLS/域名和生产权限需要在目标环境中单独验收。
