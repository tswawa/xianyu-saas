# 安全政策与漏洞提报

本文档说明 xianyu-saas 的安全机制、运行边界与漏洞提报途径。

## 报告安全漏洞

如果在本项目中发现安全漏洞（如越权访问、凭据泄露风险、发货逻辑缺陷等），请不要在公开 Issue 或讨论区披露细节。

可通过以下途径提交私密安全报告：
1. **GitHub 安全通告**：在代码仓库页面通过 **Security -> Advisories -> Report a vulnerability** 提交私密报告。
2. **私密邮件反馈**：发送邮件至维护者邮箱 `tswawa9595@gmail.com`。

报告时请包含以下信息：
- 受影响的模块、接口或配置项；
- 漏洞成因简述与脱敏复现步骤；
- 修复建议或临时缓解措施。

维护团队会在收到报告后进行核实、评估并跟进修复。

## 系统安全机制与边界

### 1. 管理员账号与注册控制
- **首次管理员注册**：在系统默认配置（`SAAS_BOOTSTRAP_ENABLED=0`）下，全新空数据库允许通过网页登录界面注册首位管理员账号。该操作不受 `SAAS_ALLOW_REGISTRATION=0` 的限制。
- **空站部署安全**：空数据库允许首个访问者直接创建管理员，部署者在完成首位管理员注册前，应避免将服务直接暴露到不受信的公网环境。
- **初始化安全与并发回滚**：新用户注册与首次管理员创建支持安全接纳既有空目录或纯默认配置残留，严格拒绝业务数据、未知配置、非默认修改、损坏文件与符号链接；两入口均具备并发回滚保护，仅在异常时清理本次新建且尚未被用户、店铺或运行时认领的目录；底层 SQLite 写事务串行化，在已有事务或数据库异常时保守保留存储目录。首次管理员与后续注册开关的权限边界保持独立。
- **后续用户注册**：首位管理员创建完成后，后续注册默认处于关闭状态。若需开放后续注册，必须在环境变量中配置 `SAAS_ALLOW_REGISTRATION=1`，且在系统设置中开启注册开关。
- **显式令牌引导**：配置了 `SAAS_BOOTSTRAP_ENABLED=1` 的运维环境，通过本地受保护的令牌文件和受信来源进行管理员创建，禁用无令牌首次注册。

### 2. 多店铺隔离与数据保护
- **进程与目录隔离**：各店铺的实时消息监听与自动化操作由独立的 Python Worker 进程承载，使用独立目录存放运行时数据，同时共享宿主机资源。单店铺发生异常退出或凭据失效时，主要影响该店铺自身，降低对其他店铺的影响。
- **存储隔离**：店铺的登录会话凭据、商品缓存、知识库、规则文件及订单记录均按店铺目录与数据库记录隔离存放。
- **权限边界**：普通店主（`owner`）仅能查看与操作自己名下的店铺，无权访问其他用户的店铺数据或平台审计日志。

### 3. 模型凭据与会话保护
- 闲鱼平台登录凭据（Cookie）保存在服务端受文件系统与账号权限严格保护的私有目录中。
- 浏览器持有工作台生成的 HttpOnly 安全会话 Cookie 维护登录状态，防止脚本读取。
- 用户在工作台配置的大模型 API Key 在前端输入并提交后，由服务端使用主密钥（`SAAS_AI_MASTER_KEY`，32 字节随机值做标准 Base64 编码）进行对称加密存储。
- 保存成功后后续接口不会回显明文 API Key，浏览器前端也不会长期缓存明文密钥，页面仅展示脱敏后的连接状态与配置元数据。
- 上游模型返回 401、403 等鉴权异常时，通过非本站 401 错误包装返回，避免前端误将大模型错误判定为系统登录会话超时而退出；测试与提问失败后操作按钮恢复可用状态，历史失败请求不污染后续新会话。
- 服务端对大模型接口请求实施 SSRF 防御，默认拦截私网地址与未受信任连接。Fake-IP 属于代理与 DNS 环境兼容问题，大模型域名需通过真实公网 DNS 解析方可通过安全检查；严禁关闭 SSRF 防御或跳过 TLS 证书校验，禁止将本机临时 IP 映射写入通用配置或发布制品。

### 4. 履约发货核验
- 自动发货前，Worker 会调用平台订单接口核验订单真实状态，比对买家 ID、卖家身份、商品 ID 以及待发货状态标识（`status == 2`）。
- 仅依据核验通过的平台订单信息执行卡密分发或链接下发，买家私信中的文字或付款截图不能作为发货依据。

### 5. 店铺助手操作边界
- 店铺助手（智能运维）仅能在授权范围内调用配置工具，用于修改当前店铺的问答知识、回复规则与发货资料。
- 助手无权执行系统 Shell 命令，不能调用退款接口，不能直接向买家发送消息，不能直接执行实际发货，也不能导出卡密内容。

### 6. 系统更新与签名验证安全
- **版本探测与无静默执行**：服务端版本发现为低频只读探测（默认 6 小时），全站共享缓存；页面仅读取本地元数据提示，绝不静默下载或自动安装更新。
- **签名校验、静态解析与可信根**：所有升级包均通过 Ed25519 数字签名保护，并在运行端依据预先可信安装的本地公钥校验；系统绝不自动信任远端随行下载的公钥。升级候选包的维护协议核验，统一在验签与文件摘要确认后通过纯 AST 静态语法解析确认 `MAINTENANCE_PROTOCOL=1`，绝不为了协议核验而执行候选代码。
- **权限最小化、Socket 隔离与 Compose 凭据保护**：在 Docker 升级模式中，Web 应用容器绝不挂载宿主机 Docker socket（`/var/run/docker.sock`）。仅独立的更新器辅助容器挂载 Docker socket；该挂载属性标记为 `read_only`，但更新器仍可借由 UNIX socket 通信调用 Docker 守护进程的高权限管理 API，更新器属于受信任的高权限核心组件。更新器容器不暴露对外网络端口与管理 API，丢弃多余 Linux 权限，仅向 Web 容器暴露受限的共享 IPC 卷。宿主机通过 `initialize` 子命令向更新器登记 Compose 部署配置时，可能包含敏感环境凭据；该 JSON 严禁打印、记录日志或提交代码仓库，通过标准输入原字节安全传输并仅保存在更新器私有卷中。
- **管理员密码再次确认与凭据保护**：升级必须由系统管理员在网页中下载校验后，显式输入当前管理员密码完成身份再次确认，并确认停机维护风险方可提交；系统不包含第二认证因素，密码与确认令牌不存入浏览器持久存储。
- **排他执行与漂移保留**：升级操作期间不支持并行执行外部容器变更或服务操作（如并发运行 `docker compose up`、手工构建、修改标签等）。若在升级执行过程中检测到无法安全恢复的外部环境漂移（如数据卷、网络或挂载身份变动），系统将保持维护状态并完整保留现场错误信息转人工排查，绝不使用旧快照覆盖运维人员的新配置。Docker `initialize` 仅支持首次受控登记，配置漂移后暂停网页升级转人工重新接入，严禁删除私有卷强行绕过。
- **数据保留与回滚红线**：常规自动回滚仅恢复代码与容器镜像，支持重新创建旧版本容器（容器 ID 可以改变，继承原配置与数据卷），**绝不使用旧备份覆盖用户业务数据**。破坏性或未经验证的迁移直接拒绝执行，转入人工处理。
- **独立更新器执行环境与准入门禁**：systemd 独立更新服务由 root 运行于独立维护目录（`SAAS_UPDATER_BUNDLE_ROOT`），必须从正确生产虚拟环境调用更新脚本；首次接入须通过 `--import-trusted-baseline` 导入已签名离线基线资产并由 AST 静态语法核验 `MAINTENANCE_PROTOCOL=1`（不自动切换 current/不触碰业务数据），人工切换现役软链接后使用与服务 `EnvironmentFile` 一致的可信路径环境显式执行 `--initialize`。只有成功生成六字段可信接入记录（`status/initialization.json`，严格匹配公钥、固定 8 文件 bundle 与 entrypoint）后控制面才放行升级操作，避免 `sudo` 环境清理导致回退至非预期的默认路径。

### 7. 代码库安全红线
- 严禁向代码仓库提交任何真实业务数据，包括闲鱼登录 Cookie、大模型 API Key、真实订单号、买家会话记录或可用卡密。
- 测试用例与文档一律使用虚拟示例数据。
