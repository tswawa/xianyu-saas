# 安全政策

本项目处理店铺连接状态、会话、订单和数字资料。安全问题请优先使用仓库 GitHub Security Advisories 的私密报告入口；不要在公开 Issue、讨论或 Pull Request 中发布可复用的攻击细节。

## 报告安全问题

请在报告中说明：

- 受影响的版本、组件或配置；
- 不包含真实数据的最小复现步骤；
- 可能影响的账号、店铺、会话或履约边界；
- 已知的缓解措施。

报告中不要包含 Cookie、Token、API Key、密码、二维码登录材料、真实订单号、买家信息、聊天正文、库存、兑换码、数据库或未经脱敏的日志。若需要传递敏感样例，请先用安全报告入口提供的私密附件功能，并只提交最小必要内容。

维护者会评估影响、修复范围和披露时间。安全报告与普通功能请求分开处理；不要通过公开讨论追踪未修复漏洞的细节。

## 不进入仓库的内容

- 服务凭据、模型密钥、支付信息和任何登录态；
- 真实买家、订单、库存、兑换码、网盘资料和数据库；
- 生产环境变量、备份、运行日志和数据卷；
- 本机协作指令、个人路径、临时导出物和未授权第三方素材。

环境变量只提交 `*.example` 模板。运行数据和密钥应由部署环境或密钥管理系统保存，并使用最小权限。

## 安全开发

默认测试使用脱敏样例、临时目录和模拟上游。真实平台账号、订单、发货、验证码和第三方模型请求只能在受控环境中验证。

提交前建议运行：

```bash
python3 tests/repository-contract.py
npm test
git diff --check
```

发现凭据进入提交历史后，应立即按对应服务的流程撤销或轮换；删除工作区文件不能使已经泄露的凭据失效。